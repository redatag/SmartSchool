# SmartSchool

SmartSchool is a .NET 10 modular monolith. The repository currently contains
Milestone 1 only: the compilable solution and project dependency skeleton.

## Run locally

Run `dotnet restore` and `dotnet build SmartSchool.slnx` from the repository root.

No domain, application, persistence, authentication, or authorization features
are implemented in this milestone.
